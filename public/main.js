function expandAbout() {
	var elements = document.getElementsByClassName("age");
	for (var i = 0; i < elements.length; i++) {
	    elements[i].style.width=(40+"%");
	    elements[i].style.height=(10+"px");
	}
}